// 3.Set -struktura podataka za skladistenje vise vrednosti koja ne dozvoljava duplikate.Slicna je nizu (Array),
// a sintaksa za pravljenje seta je:

set = new Set(["Asija", "Melida", "Dino", 21, 24, "Dino"]);
// ponavljanje elemenata se nece primetiti
console.log(set);

// 4. Map - struktura podataka za skladistenje vise vrednosti i slicna je objektu
// Sintaksa za pravljenje mapa:

map = new Map([
  ["banane", 160],
  ["jabuke", 160],
  ["sljive", 160],
  ["kruske", 160],
  ["maline", 160],
]);

console.log(map);
