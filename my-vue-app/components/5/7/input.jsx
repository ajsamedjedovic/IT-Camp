const Input = ({ type, className, style, onChangeText, placeholder }) => {
  return (
    <div>
      <input type="text" style={style} onClick={onComplete}></input>;
    </div>
  );
};

export default Input;
